# Desktop GUI dependencies (optional — not needed for the CLI).
# Install alongside the base requirements:  pip install -r requirements-gui.txt
-r requirements.txt
PySide6>=6.6  # cross-platform Qt desktop UI (macOS & Windows)
